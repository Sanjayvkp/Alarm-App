class Alarm {
  final int id;
  final String title;
  final String
      time; // You can store time as a string or DateTime object, depending on your preference

  Alarm({
    required this.id,
    required this.title,
    required this.time,
  });
}
